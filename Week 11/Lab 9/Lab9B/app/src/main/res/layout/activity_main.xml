<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center">

    <!-- TextView for Temperature Sensor -->
    <TextView
        android:id="@+id/temperature"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Temperature Sensor: Not available"
        android:textSize="20sp" />

    <!-- TextView for Proximity Sensor -->
    <TextView
        android:id="@+id/proximity"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Proximity Sensor: Not available"
        android:textSize="20sp" />

    <!-- TextView for Light Sensor -->
    <TextView
        android:id="@+id/light"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Light Sensor: Not available"
        android:textSize="20sp" />

    <!-- TextView for Pressure Sensor -->
    <TextView
        android:id="@+id/pressure"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Pressure Sensor: Not available"
        android:textSize="20sp" />

</LinearLayout>
